# infra/kubernetes/neo4j.yaml
# Neo4j StatefulSet + PersistentVolumeClaim for Monolith Breaker
#
# FUTURE DEVELOPMENT:
# - Deploy Neo4j 5.x Community Edition as a StatefulSet (single replica for dev).
# - PersistentVolumeClaim for /data (graph storage) — survives pod restarts.
# - Environment variables:
#     - NEO4J_AUTH=neo4j/password (override via K8s Secret in production)
#     - NEO4J_PLUGINS=["graph-data-science"] for Louvain clustering
# - Expose ports:
#     - 7474 (HTTP browser)
#     - 7687 (Bolt protocol for driver connections)
# - Resource requests: cpu 500m, memory 1Gi (minimum for GDS plugin).
# - Readiness probe on HTTP port 7474.
# - Create a ClusterIP Service for internal access by the backend.
#
# apiVersion: apps/v1
# kind: StatefulSet
# ...
#
# apiVersion: v1
# kind: PersistentVolumeClaim
# ...
#
# apiVersion: v1
# kind: Service
# ...
