{# backend/scaffolding/templates/python_fastapi/main.py.j2 #}
{# 
Python FastAPI — main.py Template

FUTURE DEVELOPMENT:
- Render the FastAPI app entry point:
    - Create FastAPI() instance with title="{{ service_name }}"
    - Include router from router.py
    - Add startup/shutdown events if needed
    - Configure CORS middleware
#}
